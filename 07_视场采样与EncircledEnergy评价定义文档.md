# 视场采样与评价接口定义文档

## 1. 文档目的

本文档不是最终操作数手册，而是后续自动化程序使用的**评价接口契约草案**。

它的作用是把用户输入、Zemax 写入、Merit Function 生成、指标回读和报告输出统一起来，避免同一个指标在不同模块中出现口径不一致。

本文档先明确激光雷达接收 / F-theta 镜头中最容易出错的两件事：

1. FOV 全角如何转换为 Zemax 视场，以及用哪些视场采样点；
2. Encircled Energy 或其他评价指标如何映射到 Zemax 操作数、单位和达标判据。

具体操作数、参数列和权重以后以你提供或确认的版本为准；如果本文档中存在系统预填操作数，只作为实现草案，需经人工确认后才能固化。

---

## 2. FOV 与视场定义

### 2.1 全角与半视场换算

用户输入统一为**全角 FOV**。写入 Zemax 前转换为半视场：

```text
半视场角 = 全角 FOV ÷ 2
```

示例：全角 60° → 半视场 30°，Zemax 最大视场设为 30°。

### 2.2 视场类型

视场类型固定为 **Angle（角度视场）**。

原因：激光雷达接收 / F-theta 镜头物方通常在无穷远，用角度定义视场符合实际，也便于 F-theta 像高—角度关系建模。

（Object Height 物高仅用于有限共轭系统，本项目第一阶段不使用。）

### 2.3 视场采样点

第一阶段采用 Y 方向对称视场采样（已确认）：

```text
归一化视场：0, 0.25, 0.5, 0.7, +1, -1
```

对应 Zemax 视场设置（以半视场角 H 为例，H = 全角 ÷ 2）：

| 序号 | 归一化 | Y 视场角 |
|---|---|---|
| 1 | 0.00 | 0 |
| 2 | 0.25 | 0.25 × H |
| 3 | 0.50 | 0.50 × H |
| 4 | 0.70 | 0.70 × H |
| 5 | +1.00 | +H |
| 6 | -1.00 | -H |

说明：

1. 保留正负 1 视场，用于检查视场对称性和边缘崩溃；
2. 0.7 视场接近能量加权中值，是像质重点；
3. 采样点写入后必须回读验证数量和数值；
4. 后续如需更密的扫描线性采样，可按等角度追加中间视场。

---

## 3. Encircled Energy 评价定义

### 3.1 操作数选择

当前草案使用 **GENC（Geometric Encircled Energy, distance）** 表达 Encircled Energy 约束；后续具体操作数以你提供或确认的版本为准。

来源：Zemax 手册 p.1597 Optimization Operands。

- GENC：给定能量比例 Frac，返回**达到该比例所需的半径**，焦面模式单位为 μm；
- GENF：给定半径，返回该半径内的**能量比例**。

两者互为反函数。用户需求通常写成“R μm 半径内能量 ≥ P”，用 GENC 表达为：

```text
GENC(Frac = P) 返回半径 r
达标条件：r ≤ R
```

示例：要求“25μm 半径内能量 ≥ 80%”，即约束 `GENC(Frac=0.8) ≤ 25μm`。

### 3.2 GENC 参数规范

| 参数 | 含义 | 第一阶段取值 |
|---|---|---|
| Samp | 光瞳采样，1=32×32，2=64×64，3=128×128 | 建议 2 起步，稳定后可升 3 |
| Wave | 波长号，0 为多波长 | 按任务：单波长填对应号，多波长填 0 |
| Field | 视场号 | 对每个采样视场分别建操作数 |
| Frac | 目标能量比例 | 按任务，例如 0.8 |
| Type | 1=encircled，2=x only，3=y only，4=ensquared | 圆形探测器用 1，方形像元用 4 |
| Refp | 参考点，0=主光线，1=质心，2=顶点，3=光斑中心 | 建议 1（质心），报告中注明 |
| No Diff Lim | 0=按衍射极限缩放，非0=不计衍射 | 第一阶段填 1（纯几何），验收阶段可另用衍射校核 |

### 3.3 单位与达标判定

1. GENC 焦面模式返回 μm，报告统一用 μm；
2. 目标写成“半径 R μm 内 ≥ Frac”，判定 `GENC(Frac) ≤ R`；
3. 每个采样视场都要评价，取最差视场作为达标判据；
4. Type、Refp、Samp、Wave 必须写入报告，保证可复现。

### 3.4 几何与衍射说明

第一阶段先用几何 EE（GENC）做优化，速度快、稳定。905nm/940nm 属近红外，衍射不可完全忽略，因此**验收阶段**可再用衍射版本（DENC/DENF）复核，作为后续扩展，不作为第一阶段优化目标。

---

## 4. F-theta 扫描线性定义（预留）

F-theta 镜头理想像高与视场角关系：

```text
理想像高 y_ideal = f × θ
（θ 为视场角，单位 rad；f 为 F-theta 焦距）
```

扫描线性误差：

```text
线性误差 = (y_actual - y_ideal) / y_ideal × 100%
```

实现要点：

1. 用实际主光线像高（可由 REAY 类操作数或光线追迹读取）与 f×θ 比较；
2. 对每个采样视场计算误差，取最大值作为扫描线性指标；
3. 具体操作数用法在实现前再查知识库确认。

第一阶段接收镜头若不要求严格 F-theta，可先把扫描线性作为记录项，不作为强约束；具体阈值由每次任务输入。

---

## 5. 当前决策与后续确认项

当前决策（已确认）：

1. FOV 全角 = 半视场 × 2；
2. 视场类型固定为 Angle；
3. Y 视场采样：0 / 0.25 / 0.5 / 0.7 / ±1；
4. Encircled Energy 第一阶段用 GENC，纯几何；
5. 需求“R μm 内 ≥ P”转换为 GENC(Frac=P) ≤ R。

后续需要你按任务提供：

1. Frac（目标能量比例）和半径 R；
2. Type 用 encircled 还是 ensquared（探测器是圆形还是方形像元）；
3. 是否要求严格 F-theta 扫描线性及其阈值。

后续需要你补充的光学工程优化思路，我会整理成“优化策略经验”写入 04 文档和经验库。
