# 项目需求与范围定义文档

## 1. 文档目的

本文档用于明确“基于 Zemax 的自动光学设计 AI 框架”的项目目标、第一阶段范围、输入输出、成功标准和暂不处理的内容，防止项目在早期发散成过大的通用平台。

---

## 2. 项目名称

**基于 Zemax 的激光雷达镜头自动设计 AI 框架**

---

## 3. 项目背景

传统光学设计高度依赖工程师经验。一个完整设计通常需要人工完成：

1. 理解需求；
2. 选择初始结构；
3. 搭建 Zemax 系统；
4. 设置波长、视场、孔径和探测器面；
5. 构建 Merit Function；
6. 释放变量；
7. 反复优化；
8. 评估光斑、能量集中度、视场覆盖、相对照度、畸变/扫描线性度等指标；
9. 根据失败情况调整策略；
10. 输出设计文件和报告。

Zemax 具备强大的光学仿真和优化能力，但自动化程度、策略复用能力和失败经验沉淀能力不足。本项目希望通过 AI、规则系统和 ZOS-API 自动化，将人工经验转化为可执行、可复盘、可迭代的激光雷达镜头自动设计流程。

---

## 4. 第一阶段目标

第一阶段目标是完成一个可运行的 MVP：

> 面向激光雷达镜头设计，输入设计需求后，系统能够自动选择初始结构，调用 Zemax 执行优化，并输出候选设计文件和评价报告。

第一阶段不追求完全通用，也不追求从零创造全新镜头结构，而是重点验证“激光雷达镜头自动设计闭环”。

当前已确认：第一阶段先做接收端激光雷达镜头，重点面向面阵接收，通常需要考虑 F-theta 特性；第二步再扩展发射端。用户会提供初始结构或已经完成的 Zemax 镜头文件作为设计起点。

---

## 5. 第一阶段范围

### 5.1 包含内容

| 模块 | 内容 |
|---|---|
| 需求解析 | 将激光雷达镜头需求转换为标准 JSON 参数 |
| 初始结构筛选 | 从结构库中选择候选激光雷达或近红外镜头 |
| Zemax 自动化 | 自动连接、加载、设置、优化、保存 |
| Merit Function 生成 | 自动配置焦距、FOV、F/# 或 NA、光斑、能量集中度、尺寸、厚度等约束 |
| 变量释放 | 支持曲率、厚度、圆锥系数、非球面项的分阶段释放 |
| 指标评价 | 自动测量 EFL、F/# 或 NA、FOV、Spot、Encircled Energy、相对照度、畸变/扫描线性度等 |
| 多方案对比 | 对多个候选设计进行评分和排序 |
| 报告输出 | 输出设计文件、指标报告、日志和 AI 复盘 |
| 经验记录 | 保存成功和失败经验，供后续复用 |

---

### 5.2 暂不包含内容

| 暂不包含内容 | 说明 |
|---|---|
| 完全从零发明镜头结构 | 第一阶段以已有结构为起点 |
| 自动增删镜片 | 涉及复杂结构重构，放到后续阶段 |
| 完整杂散光分析 | 第一阶段只记录风险，不做完整自动化 |
| 完整公差自动优化 | 可作为第二阶段或第三阶段扩展 |
| 激光安全等级计算 | 需要系统级发射功率和法规输入 |
| 扫描机构联动仿真 | 待确认是否为扫描式系统 |
| 多物理场热结构耦合 | 暂不处理温漂、结构变形等因素 |

---

## 6. 输入参数规范

第一阶段建议支持以下输入参数。

| 参数 | 示例 | 说明 |
|---|---|---|
| lens_type | lidar_receiver | 镜头类型：接收端/发射端/收发一体/扫描镜头 |
| wavelength_nm | 905 | 工作波长 |
| wavelength_band_nm | 895-915 | 波段范围，可选 |
| efl_mm | 12 | 有效焦距，可选但建议提供 |
| f_number | 1.8 | 目标 F/#，接收端常用 |
| numerical_aperture | 0.28 | NA，可替代 F/# |
| fov_deg | 30 | 全视场或半视场需明确 |
| detector_type | SPAD | 探测器类型 |
| detector_size_mm | 1.0 | 探测器有效尺寸 |
| pixel_pitch_um | 10 | 面阵探测器像元尺寸，可选 |
| spot_limit_um | 50 | 最大允许光斑尺寸 |
| encircled_energy_target | 0.8 | 能量集中度目标 |
| encircled_energy_radius_um | 25 | 能量统计半径 |
| ttl_max_mm | 30 | 总长上限 |
| max_outer_diameter_mm | 20 | 最大外径 |
| bfl_mm | 5 | 后焦或探测器安装距离 |
| distortion_limit_percent | 5 | 畸变限制，按场景决定重要性 |
| relative_illumination_min | 0.6 | 边缘相对照度下限 |
| asphere_allowed | true | 是否允许非球面 |
| material_scheme | all_glass / glass_plastic / glass_glass_asphere | 材料组合：全玻璃、玻塑混合、玻璃+玻璃非球面 |
| plastic_thermal_analysis_required | true | 使用塑料镜片时是否需要热分析 |
| material_constraint | nir_transmissive | 材料限制 |

标准输入示例：

```json
{
  "task_name": "905nm_SPAD_接收端_Ftheta_001",
  "lens_type": "lidar_receiver_f_theta_area_array",
  "start_file": "D:/lens_library/example.zos",
  "wavelength_nm": 905,
  "wavelength_band_nm": "890-920",
  "fov_full_deg": 60,
  "efl_mm": 12,
  "f_number": 1.8,
  "numerical_aperture": null,
  "detector_type": "SPAD",
  "detector_size_mm": 1.0,
  "pixel_pitch_um": 10,
  "spot_limit_um": 50,
  "encircled_energy_target": 0.8,
  "encircled_energy_radius_um": 25,
  "scan_linearity_limit_percent": 2,
  "relative_illumination_min": 0.6,
  "ttl_max_mm": 30,
  "max_outer_diameter_mm": 20,
  "bfl_mm": 5,
  "material_scheme": "all_glass",
  "asphere_allowed": true,
  "plastic_thermal_analysis_required": false,
  "material_constraint": "nir_transmissive",
  "priority": ["spot", "encircled_energy", "scan_linearity", "ttl"],
  "notes": "FOV 为全角；第一阶段不做非序列。"
}
```

如果不想写 JSON，也可以按下面的表格形式给我：

```text
任务名称：905nm SPAD 接收端 F-theta 镜头
初始文件：D:\lens_library\example.zos
镜头类型：接收端 / 面阵接收 / F-theta
工作波长：905±15nm
FOV：60° 全角
目标焦距：12mm
F/#：1.8
NA：无
探测器：SPAD
探测器尺寸：1.0mm
像元尺寸：10μm
Spot 要求：≤50μm
Encircled Energy：25μm 半径内 ≥80%
扫描线性：≤2%
相对照度：≥60%
TTL：≤30mm
最大外径：≤20mm
BFL：5mm
材料方案：全玻璃
是否允许非球面：允许
是否包含塑料：否
优化优先级：Spot > Encircled Energy > 扫描线性 > TTL
备注：第一阶段不做非序列。
```

最简单也可以直接用自然语言描述，但必须包含：波长、FOV 全角、焦距或 F/#/NA、探测器、优化目标、机械约束、初始文件。

---

## 7. 输出交付物

每次设计任务完成后，系统应输出：

1. Zemax 设计文件；
2. 优化前后指标对比表；
3. 设计参数总表；
4. 表面参数表；
5. 材料表；
6. 变量设置表；
7. Merit Function 操作数表；
8. Spot Diagram 结果；
9. Encircled Energy 或探测器面能量统计结果；
10. 视场覆盖结果；
11. 相对照度结果；
12. 畸变或扫描线性度结果；
13. 透过率和材料风险记录；
14. 优化日志；
15. 失败或成功原因分析；
16. AI 复盘报告。

如果第一阶段最后确认为成像式面阵激光雷达，也可增加 MTF 分析结果。

---

## 8. 设计成功判据

第一阶段建议使用如下验收标准，具体数值需要你确认。

| 指标 | 建议标准 |
|---|---|
| 工作波长 | 与目标波长一致 |
| EFL | 目标值 ±1% 或满足视场/探测器匹配 |
| F/# 或 NA | 满足接收通光量或发射准直要求 |
| FOV | 覆盖目标视场或扫描角 |
| Spot | 小于探测器尺寸、像元尺寸或指定阈值 |
| Encircled Energy | 指定半径内能量比例达标 |
| Relative Illumination | 边缘视场不低于下限 |
| Distortion / Scan Linearity | 满足系统标定或扫描要求 |
| Ray Trace | 全视场、全波长可追迹 |
| TTL / 外径 | 满足机械包络 |
| BFL | 满足探测器安装空间 |
| Center Thickness | 大于制造下限 |
| Edge Thickness | 大于制造下限 |
| Air Gap | 无负值或过小间隔 |
| 材料 | 近红外透过率和可制造性满足要求 |

---

## 9. 已确认信息与每次设计需输入内容

当前已确认：

1. 第一阶段先做接收端，后续再做发射端；
2. 接收端按面阵接收优先考虑，探测器优先为 SPAD，其他镜头可能为 CMOS；
3. 激光雷达镜头通常需要考虑 F-theta 特性；
4. 工作波长支持 905±15nm 或 940±15nm；
5. FOV 由你提供全角；
6. 材料组合包括全玻璃、玻塑混合、玻璃+玻璃非球面；
7. 使用塑料镜片时需要考虑热分析；
8. 你会提供初始结构、已完成镜头或资料。

每次具体设计时仍需要你输入：

1. 本次使用 905nm 还是 940nm 波段；
2. 全视场 FOV 数值；
3. 目标焦距、F/# 或 NA；
4. 探测器有效尺寸、像元尺寸或接收面定义；
5. 优化目标，例如 Spot、Encircled Energy、扫描线性、相对照度、体积、成本等；
6. 机械约束，例如 TTL、最大外径、BFL、装配空间；
7. 材料方案和是否允许塑料件；
8. 初始 Zemax 文件或参考结构。

后续开发中，如果某个设计选择会影响系统方向，我会继续提示你确认。

---

## 10. 项目验收标准

MVP 完成时，应满足：

1. 能连接 Zemax 并执行自动化操作；
2. 能加载至少 3 个候选初始结构；
3. 能自动生成激光雷达镜头 Merit Function；
4. 能执行至少一条完整优化路线；
5. 能自动输出核心指标；
6. 能对候选方案排序；
7. 能保存最终 Zemax 文件；
8. 能输出设计报告；
9. 每次运行有完整日志；
10. 失败案例能被记录。

---

## 11. 后续扩展方向

MVP 完成后，可逐步扩展：

1. 发射端和接收端联合优化；
2. 扫描 F-theta 镜头优化；
3. 自动玻璃替换；
4. 杂散光和鬼像分析；
5. 公差分析；
6. 温漂评估；
7. 批量实验系统；
8. 多镜头类型支持；
9. 更复杂的 AI 策略规划；
10. 自学习经验库。
