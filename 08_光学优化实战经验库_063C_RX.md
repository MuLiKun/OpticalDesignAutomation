# 光学优化实战经验库（063C_B0_RX 四片式接收镜头全程复盘）

> 来源：063C_B0_RX-20260311 四片式 F/1.27 激光雷达接收镜头，从 baseline 到 017 共 17 轮优化的实测经验。
> 所有结论均经 ZOS-API（Ansys Zemax OpticStudio 2023 R1，standalone 模式）实测验证；引用行号/面号针对本项目结构（面3-10 为四片镜，面12/14 为滤光片+保护窗平板组，面17 为像面）。
> 配套可复用脚本见 `output/workdir/_*.py`（§8）。

---

## 1. ZOS-API 稳定性范式（最高优先级，违反必崩）

后台 standalone 模式下 OpticStudio 会**静默崩溃**：python 进程正常退出（exit 0）、无异常抛出，但优化结果丢失、日志停在中途。以下规则全部由踩坑得出：

| # | 规则 | 症状（违反时） |
|---|---|---|
| 1 | **每个 python 进程只开一次优化工具**（DLS 或 Hammer），跑完立即 `SaveMeritFunction` + `SaveAs` 再退出进程；多轮优化用多次进程调度 | 同进程第二次 `OpenLocalOptimization()` 后静默崩溃 |
| 2 | DLS 用 **Fixed_10_Cycles / Fixed_50_Cycles 短循环**；`Automatic` 在病态起点（如干涉恢复、MF>1）会长跑并崩溃 | 日志停在 running_dls，文件未更新 |
| 3 | **MFE 中不要放 MTFT/MTFS 操作数行**再进后台 DLS；MTF 用点列图+TRAY 代理优化，验证阶段再用 `GetOperandValue` 逐点读 | 含 MTF 行的 MFE 后台 DLS 必崩 |
| 4 | Hammer 正确姿势：`tool.Run()` → `time.sleep(秒)` → `tool.Cancel()` → `tool.WaitForCompletion()` → `Close()`；**运行中不要轮询 `IsRunning`/`CurrentMeritFunction`**，`RunAndWaitWithTimeout` 也会崩 | 轮询工具属性数次后崩溃 |
| 5 | 崩溃后**文件可能已被后台保存**（Hammer 尤其如此）——重开进程用 `_probe_mf.py` 实测 MF 判断成果是否保留，勿盲目重跑 | 误以为白跑，实际 MF 已改善 |
| 6 | 关键节点（约束建完、每轮优化后）**立即 SaveAs**，把崩溃损失限制在一轮以内 | 一次崩溃丢掉全部约束重建工作 |

## 2. MFE 操作数编写模板（实测可用写法）

### 2.1 通用 config_row（Int/Double 自动回退）

单元格类型因操作数而异（如 RSCE cell4 是 Double、CTGT cell2/3 是 Int），写错抛 `Expected Integer, got 'Double'`。通用写法：先按预期类型写，失败换另一种：

```python
cell = op.GetCellAt(idx)
for use_int in ([True, False] if prefer_int else [False, True]):
    try:
        cell.IntegerValue = int(v) if use_int else None or ...
        # 简化：try Int 再 try Double
        break
    except Exception:
        continue
```

### 2.2 f-theta 畸变标准链（每视场 5 行）

```
CENY  wave=主波长, field=f, 面=像面    ← 读当前主光线像高 (mm)
CONS  Target = EFL_target × θ(rad)     ← 理想像高常数
DIFF  引用上两行                        ← 偏差 (mm)，Value 直接可读
PROB  引用 DIFF 行, 倍数=1000           ← mm → μm
ABLT  引用 PROB 行, Target=9.9, w=3.0  ← |偏差| < 9.9 μm 才惩罚
```

**坑**：PROB 的倍数单元写 `IntegerValue` 无效（值恒 0），必须 `GetCellAt(3).DoubleValue = 1000.0`。写完必须自校验 `PROB == DIFF×1000`，不等就换单元格重试（`prob_factor_check` 模式）。
**特性**：ABLT 在限内零惩罚 → 优化器会把偏差**精确压满边界**（F5/F6 = 9.900）。需要安全余量就把 Target 收紧（9.9→9.5）。

### 2.3 可制造性逐面对约束（勿用全局区间操作数）

用户明确要求逐面写，且逐面行公差 TSC 可按行 REPORT：

```
每片镜 (s1,s2)=(3,4)/(5,6)/(7,8)/(9,10):
  CTGT s1 s2  0.8   w2   中心厚下限
  CTLT s1 s2  7.0   w2   中心厚上限
  ETGT s1 s2  0.8   w2   边缘厚下限
每个空气段 (s1,s2)=(4,5)/(6,7)/(8,9)/(10,11):
  CTGT s1 s2  target w5  中心空气
  ETGT s1 s2  target w5  边缘空气      ← 中心+边缘双控，min 才是真实最小间隔
特殊: CTGT 9 10 1.5 w10  （L4 中心厚硬要求）
```

**坑1**：判线卡线——DFA 判 `≥0.8` 时优化收敛在 0.7999 会 fail，约束 target 要留数值余量（0.85）。
**坑2**：ETGT 行保存回 .mf 后 cell3 显示的数字可能与写入不同（行引用式存储），**约束是否生效以 `GetOperandValue(ETLT,...)` 实测为准**。

### 2.4 像差定向操作数（TRAY，MTF 不平滑救星）

F5/F6 子午 MTF 塌陷（T-S 分离 0.18）时，直接压子午边缘光线：

```
TRAY  wave=主波长, Hx=0, Hy=视场归一化, Px=0, Py=±1   Target=0, w=2.0
```

Hy/Px/Py 单元是 Double。效果实测：TRAY(Py+1) 从 -0.171 → -0.0005（340×），T-MTF 0.675→0.717。
同时给其他视场挂低权重（w=0.5）守护行，防拆东墙补西墙——本项目 F2-F4 GEO 同轮全部下降，仅 F1 从 11.3 回吐到 15.0（仍为全场最好）。

### 2.5 EFL/BFL 括号约束

```
EFFL (w=0 只读) → OPGT 引用行 target=下限 / OPLT 引用行 target=上限
TTHI 10 11 (w=0) → OPGT 引用行 target=BFL下限
```

BFL 要"尽量大"时直接把 OPGT target 推到期望值（2.05），比软目标收敛快且稳。

### 2.6 自定义行的位置：必须在 DMFS 上方

用 `mfe.InsertNewOperandAt(dmfs_row)` 批量插行。DMFS 之后是向导默认点列图区，重开向导只重建 DMFS 之后部分，**上方自定义约束不会被覆盖**（append 到底部的写法会被向导抹掉，001~006 轮的教训）。

`find_dmfs_row` 必须带双回退：`GetOperandTypeName()` 失败再试 `.Type`，否则某些状态下找不到 DMFS。

## 3. 多重结构（MCE）决策树与操作数写法

### 3.1 何时上多重结构

```
单结构优化收敛(DLS+Hammer 均无改善)
└─ 需求是"多工况同时好"? (通焦/多温/变焦/多波段)
   ├─ 否 → 回到单结构调权重/换操作数
   └─ 是 → MCE 多重结构
        ├─ 通焦 MTF: THIC 像面前空气面, config=0/±δ
        ├─ 多温: TEMP/PRES 操作数
        └─ 变焦: THIC 变焦空气段
```

### 3.2 MCE 端建结构（实测代码）

```python
mce = sys_obj.MCE
while mce.NumberOfConfigurations < 3:
    mce.AddConfiguration(False)
mc_op = mce.AddOperand()
mc_op.ChangeType(ZOSAPI.Editors.MCE.MultiConfigOperandType.THIC)
mc_op.Param1 = 16                                   # 作用面号
mc_op.GetOperandCell(1).DoubleValue = 0.0           # config1 名义
mc_op.GetOperandCell(2).DoubleValue = +0.05         # config2 +50μm
mc_op.GetOperandCell(3).DoubleValue = -0.05         # config3 -50μm
```

### 3.3 MFE 端逐 config 约束（CONF 操作数）

`CONF` 是开关型操作数：**其后所有行都在该 config 下评估，直到下一个 CONF**。

```
CONF 1                 ← 约束块开头：全部业务约束在名义结构下评估
EFFL/OPGT/.../RSCE ...
CONF 2                 ← 切到 +50μm 结构
RSCE field=5 ...       ← 只惩罚该离焦处最差的视场（定向）
RSCE field=6 ...
CONF 3                 ← 切到 -50μm 结构
RSCE field=1 ...
CONF 1                 ← ★必须复位！忘记复位则后续行(含DMFS前插入的新行)全跑错结构
DMFS                   ← 向导区(向导 Configuration=0 时逐结构生成点列图)
```

**坑**：从单结构文件分支后重开向导，旧约束块会变成 CONF/BLNK 空行残骸留在文件里（无害但脏，且会干扰公差 TSC 的 REPORT 行号计数）。分支后应清理或重建整个 MFE。

### 3.4 通焦优化的物理极限（本项目实测定论）

- 多重结构 + 定向 RSCE 能**平移**通焦包络（+z 侧 0.20→0.24@±50μm），**不能加宽**（≥0.35 窗口宽度 73μm 优化前后不变）；
- F/1.27 四分之一波长焦深 = ±2λF² ≈ ±29 μm；要求 ±50μm@0.35 需要 ≥100μm 包络，超球面四片式物理极限约 35%；
- 名义焦面 MTF 已达衍射限 75% 时，没有多余像质可换焦深。**结论：包络宽度是孔径与面型自由度决定的，规格谈判（±35μm@0.35）或上非球面才是正解。**

## 4. 口径与材料策略

### 4.1 净口径统一（结构件要求）

- **全部面强行统一净口径 = 灾难**：010 轮全部 8.8 固定后 GEO 从 19→38μm、MTF 0.72→0.44，凹面处边缘空气约束在大口径处苛刻到挤死形状自由度；
- **正确做法（用户工艺规则）**：凸面（前表面 R>0 / 后表面 R<0）统一净口径固定（`SemiDiameter = x` 赋值即固定），凹面/平面只统一机械外径（`MechanicalSemiDiameter = x`），光学口径保留自动；
- 判凸凹：前表面 R>0 凸、后表面 R<0 凸，|R|>1e8 视为平面；
- 空气间隔真实最小值 = **min(中心 TTHI, 边缘 ETLT)**，两者都要约束（011 轮起 target=1.25）。

### 4.2 材料（Hammer 换料管控）

- 玻璃处于 Substitute 状态时 Hammer 会自动换料，可能选出高价料（H-ZF88/K4A/H-ZLAF71AGT 被用户否决）；
- **锁料**：`surf.MaterialCell.MakeSolveFixed()`，Hammer 后必须回读 `surf.Material` 校验；
- 激光雷达单色（905nm）选料原则：**折射率优先，阿贝数权重低**；平价高折可用 **H-ZLAF72A**（n≈1.889）；
- 手工换料后按 `(n_new-1)/(n_old-1)` 缩放该镜两面半径保光焦度，再 DLS，收敛快且不易跑飞。

### 4.3 后段平板区（滤光片/保护窗）

- 平板间空气是**结构固定值**（本项目面13=1.15、面11=0），必须 `ThicknessCell.MakeSolveFixed()` 后赋值；
- **教训**：留成变量会被优化器压成负值（012/013 曾出现 -0.19/-0.075 的非法几何），且不易察觉——每轮结束应检查全部厚度非负。

## 5. 优化路线决策树（全程复盘）

```
0. 基线评测(batch_validate) → 确定缺口
1. 约束体系搭好(全部在 DMFS 上方) + DLS 短循环
   └─ 收敛不达标 → 
2. 定向操作数: 找最差项(逐视场/逐面诊断) → TRAY/RSCE/权重再平衡 + DLS
   └─ 例: F5/F6 子午像差 TRAY 一轮解决 GEO+GENC+MTF 三指标 (009)
   └─ 仍卡 →
3. Hammer(限时180s, 锁料按需) —— 强约束改变后(如口径统一)最有效
   └─ 例: 011 轮 MF 0.0403→0.0030 (13×)
   └─ 多工况需求 →
4. MCE 多重结构(§3)
   └─ 物理极限 → 规格谈判 / 非球面 / 结构升级
```

配套调参经验：
- 视场权重：瓶颈视场加倍(F3/F4=3.0)，边缘不放任(F5/F6≥1.5)，轴上可让(F1=2.5)；
- 每轮只改一类东西（约束/权重/材料/口径），否则无法归因；
- 每轮正式复测口径统一用 `batch_validate_rx_lenses`（自研 spot_per_field 的 GetGeoSpotSizeFor 与正式口径有差异，只用于同轮前后对比）。

## 6. 失败案例（勿重蹈）

| 案例 | 现象 | 根因 | 修复 |
|---|---|---|---|
| 001-006 约束被向导抹掉 | 重开向导后自定义行消失 | 约束 append 在 DMFS 之后 | 全部插在 DMFS 上方 |
| 010 全口径统一 | GEO 38μm/MTF 0.44 崩盘 | 凹面大口径+边缘空气约束互斥 | 凸面统一/凹面只统一外径 (011) |
| PROB 倍数为 0 | f-theta 链 ABLT 永远满足 | 倍数单元 IntegerValue 写入无效 | DoubleValue + 自校验 |
| L3 边厚 0.7999 DFA fail | 复测 fail 但约束"满足" | 判线卡线无数值余量 | target 加余量 (0.85) |
| 平板区负厚度 | 面11/13 厚度为负 | 结构固定值留成了变量 | MakeSolveFixed + 固定值 |
| Hammer 偷换贵料 | DFA 材料 warn/fail | GLAS Substitute 未锁 | MakeSolveFixed + 事后回读校验 |
| MTF 操作数崩溃 | 后台 DLS 静默死 | MFE 含 MTFT/MTFS 行 | 点列图+TRAY 代理 |
| 定向 RSCE cell 类型 | Expected Integer 报错 | RSCE cell4 是 Double | Int/Double 自动回退 |

## 7. 关键指标演进（golden case 记录）

| 轮次 | 关键动作 | GEO max | GENC95 worst | MTF@17 | BFL | 状态 |
|---|---|---:|---:|---:|---:|---|
| baseline | — | ~30 | fail | — | — | fail |
| 008 | f-theta 链 + 逐面制造 | 25.3 | 26.2 (4/6) | 0.675 | 1.70 | 2 fail |
| 009 | TRAY 像差突破 | 19.2 | 20.4 全过 | 0.717 | 1.70 | **首个 0 fail** |
| 011 | 凸面口径统一 + Hammer | 17.2 | 17.3 | 0.749 | 1.70 | 0 fail |
| 012/013 | 双材料方案定型 | 16.4/18.6 | 14.5/17.6 | 0.778/0.711 | 1.70/2.09 | 0 fail |
| **014/015** | 平板固定 + BFL>2 | 18.3/18.5 | 15.5/17.3 | 0.715/0.702 | **2.049/2.050** | **最终候选** |
| 016/017 | 3-config 通焦 | 名义指标保持 | — | 0.727/0.683 | 2.05 | 通焦 ±35μm@0.35 |

## 8. 可复用工具脚本（output/workdir/）

| 脚本 | 用途 | 用法 |
|---|---|---|
| `_dls_once.py` | 单轮 DLS + 保存（每进程一次） | `python _dls_once.py <zmx> [Fixed_50_Cycles]` |
| `_hammer_once.py` | 限时 Hammer + 材料回读校验 | `python _hammer_once.py <zmx> [秒]` |
| `_probe_mf.py` | 只读探针：MF/EFL/材料 | `python _probe_mf.py <zmx>` |
| `_mtf_scan.py` | 只读扫描：0-17lp 频率曲线 + ±50μm 通焦 | `python _mtf_scan.py <zmx>` |
| `_defocus_setup.py` | 3-config 通焦结构 + 全约束重建 + DLS | `python _defocus_setup.py <src> <dst>` |
| `_defocus_targeted.py` | 逐 config 定向 RSCE 追加 | `python _defocus_targeted.py <zmx>` |
| `_fix_backend_and_bfl.py` | 平板区固定 + BFL 目标上推 | `python _fix_backend_and_bfl.py <src> <dst>` |

公差分析另见 `toltool/` 包与 `公差分析程序_使用说明.md`（TSC 分项 REPORT 行号需对准 MFE 行号——注意 §3.3 的 CONF/BLNK 残骸会干扰计数）。
