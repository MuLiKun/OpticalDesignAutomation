# 系统总体架构设计文档

## 1. 架构目标

本系统架构需要满足以下目标：

1. 稳定控制 Zemax；
2. 支持激光雷达镜头从需求到设计文件的端到端自动化；
3. 支持 AI 决策但避免 AI 直接乱改底层参数；
4. 支持激光雷达核心指标评价和验收；
5. 支持失败回退和日志复盘；
6. 支持经验库持续积累；
7. 后续可扩展到其他光学系统。

---

## 2. 总体架构图

```text
用户需求输入
  ↓
需求解析模块
  ↓
初始结构库 / 经验库
  ↓
AI 策略规划模块
  ↓
Zemax 自动化执行器
  ↓
指标评价模块
  ↓
报告生成与经验写回模块
```

---

## 3. 模块说明

## 3.1 需求解析模块

### 职责

需求解析模块负责将用户输入的自然语言、表格或 JSON 转换为标准激光雷达镜头设计任务。

### 输入示例

```text
设计一个 905nm 激光雷达接收镜头，全视场 30°，F/1.8，探测器有效尺寸 1mm，希望光斑小于 50μm，25μm 半径内能量大于 80%，总长小于 30mm。
```

### 输出示例

```json
{
  "task_id": "lidar_receiver_001",
  "lens_type": "lidar_receiver",
  "targets": {
    "wavelength_nm": 905,
    "efl_mm": 12,
    "f_number": 1.8,
    "fov_deg": 30,
    "detector_size_mm": 1.0
  },
  "constraints": {
    "spot_limit_um": 50,
    "encircled_energy_target": 0.8,
    "encircled_energy_radius_um": 25,
    "ttl_max_mm": 30,
    "relative_illumination_min": 0.6
  }
}
```

### 注意事项

1. 所有参数必须带单位；
2. 用户输入的 FOV 默认按全角处理，写入 Zemax 时需要转换为对应半视场；
3. 第一阶段默认镜头类型为接收端 / 面阵接收 / F-theta 相关镜头；
4. 缺失的关键参数要提示用户补充；
5. 每次设计的目标焦距、F/#、NA、优化指标和机械约束由当次任务单独输入；
6. 默认值必须显式记录；
7. 原始需求文本应保留，便于复盘。

---

## 3.2 初始结构库模块

### 职责

初始结构库负责存储和筛选候选激光雷达镜头或近红外镜头结构。

### 数据来源

1. 激光雷达专利镜头；
2. 近红外接收镜头；
3. 扫描 F-theta 镜头；
4. 历史项目；
5. Zemax 示例文件；
6. 公开论文或公开案例；
7. 人工整理的高质量结构。

### 筛选维度

| 维度 | 说明 |
|---|---|
| 镜头类型 | 接收端、发射端、收发一体、扫描 F-theta、面阵接收 |
| 工作波长 | 905nm、940nm、1550nm 等 |
| 焦距接近度 | 与目标 EFL 的匹配程度 |
| F/# 或 NA 接近度 | 与通光量要求的差距 |
| FOV 接近度 | 与目标视场或扫描角匹配程度 |
| 探测器匹配 | 像面尺寸、光斑和探测器关系 |
| TTL / 外径 | 是否满足机械包络 |
| 非球面 | 是否支持非球面要求 |
| 材料 | 是否适合近红外波段 |
| 历史成功率 | 过往优化是否容易成功 |

---

## 3.3 AI 策略规划模块

### 职责

AI 策略规划模块负责根据需求、候选结构和当前优化结果，决定下一步动作。

### 典型决策

1. 选择哪个初始结构；
2. 是否进行比例缩放；
3. 是否调整视场；
4. 是否调整 F/# 或 NA；
5. 是否优先优化 Spot；
6. 是否优先优化 Encircled Energy；
7. 是否增强边缘视场权重；
8. 是否释放非球面圆锥系数；
9. 是否释放高阶非球面项；
10. 是否加强机械尺寸约束；
11. 是否回退；
12. 是否更换初始结构。

### 输出示例

```json
{
  "strategy": "lidar_receiver_spot_energy_balance",
  "steps": [
    {"stage": "baseline", "variables": "basic", "primary_goal": "ray_trace_and_fov"},
    {"stage": "spot_control", "variables": "radius_thickness", "primary_goal": "spot_limit"},
    {"stage": "energy_control", "variables": "basic_plus_conic", "primary_goal": "encircled_energy"},
    {"stage": "package_control", "variables": "limited", "primary_goal": "ttl_and_diameter"}
  ]
}
```

### 约束

AI 只能输出结构化计划，不能直接修改 Zemax 内部对象。实际执行由 Zemax 自动化执行器完成。

---

## 3.4 Zemax 自动化执行器

### 职责

Zemax 自动化执行器是系统底层核心，负责真实操作 Zemax。

### 核心接口

```text
connect_zemax()
load_lens_file()
create_new_system()
set_aperture()
set_fields()
set_wavelengths()
set_detector_or_image_plane()
set_surface_data()
set_materials()
set_variables()
build_merit_function()
run_optimization()
evaluate_system()
save_lens()
export_analysis()
```

### 关键原则

每个写入动作后必须回读验证。

示例：

```text
set_fields() → get_fields() → count == expected_count
set_wavelengths() → get_wavelengths() → wavelength == expected_nm
set_detector_or_image_plane() → get_image_surface() → position == expected
add_surface() → get_surface_count() → count increased
```

---

## 3.5 指标评价模块

### 职责

指标评价模块负责读取 Zemax 分析结果，统一单位，并判断设计是否达标。

### 输出示例

```json
{
  "wavelength_nm": 905,
  "efl_mm": 12.1,
  "f_number": 1.82,
  "fov_deg": 30.0,
  "spot_rms_um": 18.5,
  "spot_max_um": 45.0,
  "encircled_energy_25um": 0.83,
  "relative_illumination_edge": 0.68,
  "distortion_percent": 2.5,
  "ttl_mm": 28.4,
  "ray_trace_pass": true,
  "pass": true
}
```

---

## 3.6 报告生成与经验写回模块

### 职责

1. 生成设计报告；
2. 输出指标对比；
3. 导出图表和表格；
4. 记录 AI 决策；
5. 记录失败原因；
6. 更新经验库。

### 每轮必须记录

| 内容 | 说明 |
|---|---|
| 输入需求 | 原始需求和结构化需求 |
| 初始结构 | 使用哪个候选镜头 |
| 优化策略 | 当前采用的优化路径 |
| 操作数表 | Merit Function 快照 |
| 表面参数表 | 优化前后表面数据 |
| 指标对比 | 优化前后结果 |
| 结论 | 成功、失败或需要继续优化 |

---

## 4. 推荐代码目录结构

```text
auto_optical_ai/
├── requirements/
├── lens_library/
├── zemax_api/
├── merit_function/
├── strategy/
├── evaluator/
├── agent/
├── experiments/
├── reports/
└── tests/
```

---

## 5. 任务状态机

建议每个设计任务采用以下状态：

```text
created
→ parsed
→ candidate_selected
→ zemax_loaded
→ baseline_evaluated
→ merit_built
→ optimizing
→ evaluated
→ passed / failed / retry
→ reported
→ archived
```

每个状态都要保存日志和关键数据，便于失败后恢复。

---

## 6. 异常处理原则

| 异常 | 处理方式 |
|---|---|
| Zemax 连接失败 | 重试或提示人工检查授权 |
| 文件加载失败 | 切换候选结构 |
| 光线不可追迹 | 回退或降低目标难度 |
| 光斑或能量指标无法达标 | 调整变量、权重或更换结构 |
| 优化发散 | 回退检查点并减少变量 |
| 操作数错误 | 导出 MFE 表并停止当前轮 |
| 单位异常 | 与 Zemax GUI 报告交叉验证 |
| 保存失败 | 重新保存并验证文件可加载 |

---

## 7. 已确认架构方向与后续确认项

当前已确认：

1. 第一阶段默认按接收端 / 面阵接收 / F-theta 相关激光雷达镜头实现；
2. 数据结构需要保留接收端和发射端字段，先实现接收端，后续扩展发射端；
3. 指标评价模块优先实现 Spot、Encircled Energy、探测器匹配、F-theta 扫描线性和工程约束，不以 MTF 为主；
4. FOV 输入按全角处理，系统内部转换为 Zemax 半视场。

当前决策：

1. 第一阶段不涉及非序列模式，不做杂散光、鬼像、接收效率的非序列自动化；
2. 第一阶段先以单任务闭环为主，批量实验作为后续能力说明，不作为当前必须项。

---

## 8. 架构总结

本架构的核心思想是：

```text
AI 负责想，规则负责约束，程序负责执行，Zemax 负责计算，评价器负责验收，经验库负责成长。
```
