# 基于 Zemax 的自动光学设计 AI 框架总体方案

## 1. 项目目标

本项目旨在构建一个基于 Zemax OpticStudio / ZOS-API 的自动光学设计 AI 框架，使系统能够从用户输入的光学设计需求出发，自动完成：

1. 需求解析；
2. 初始结构筛选；
3. Zemax 系统搭建；
4. Merit Function 构建；
5. 自动优化；
6. 指标评价；
7. 多方案对比；
8. 设计报告输出；
9. 经验沉淀与策略迭代。

第一阶段聚焦于：

> **激光雷达镜头自动设计 MVP**

初期不追求通用光学设计平台，而是先围绕激光雷达镜头建立稳定的 Zemax 自动化设计闭环。

---

## 2. 第一阶段应用场景

第一阶段限定为激光雷达镜头。激光雷达镜头与普通成像镜头、投影镜头不同，更关注：

1. 工作波长，例如 905nm、940nm、1550nm；
2. 发射端、接收端或收发一体结构；
3. 视场角或扫描角；
4. 光斑尺寸；
5. 能量集中度；
6. 探测器尺寸匹配；
7. 入瞳 / 出瞳 / F/# / NA；
8. 相对照度或边缘能量；
9. 材料在近红外波段的透过率；
10. 杂散光、鬼像和镀膜风险；
11. 机械包络和环境可靠性。

因此，第一阶段的成功标准不能照搬投影镜头的 MTF、畸变、远心度体系，而应以激光雷达实际光学指标为主。

---

## 3. 核心原则

### 3.1 先自动化，再智能化

第一阶段的核心是先把 Zemax 自动化执行链路做稳定。只有程序能够可靠连接 Zemax、设置系统、构建优化目标、读取结果并保存文件，AI 决策才有实际意义。

### 3.2 先做接收端，再扩展发射端

第一阶段已确认优先顺序：

1. 先做接收端激光雷达镜头；
2. 接收端以面阵接收为主，探测器优先按 SPAD，其他镜头可支持 CMOS；
3. 激光雷达镜头通常需要考虑 F-theta 特性和扫描线性；
4. 后续再扩展发射端准直 / 扩束镜头；
5. 用户会提供初始结构或已经完成的 Zemax 镜头文件作为起点。

因此第一阶段不从零发明结构，而是围绕已有接收端 / F-theta / 面阵接收结构建立自动优化闭环。

### 3.3 AI 负责决策，程序负责执行

AI 不直接随意修改 Zemax 底层参数，而是调用封装好的工具接口。底层操作必须确定、可测试、可回读、可复盘。

### 3.4 所有 Zemax 操作必须回读验证

不能“写完就信”。包括视场、波长、表面、材料、变量、操作数、优化结果和保存文件，都必须回读确认。

---

## 4. 第一阶段 MVP 范围

第一阶段 MVP 定义为：

> 输入一个激光雷达镜头设计需求，系统从候选结构库中选择若干起始结构，自动调用 Zemax 完成优化，并输出候选设计文件和评价报告。

### 4.1 MVP 必须包含

| 模块 | 内容 |
|---|---|
| 需求解析 | 将激光雷达镜头需求转换为标准结构化参数 |
| 初始结构筛选 | 从结构库中选择同类型候选起点 |
| Zemax 自动连接 | 通过 ZOS-API 控制 OpticStudio |
| 系统设置 | 设置波长、视场、孔径、探测器面、材料、变量 |
| Merit Function 生成 | 自动生成焦距、视场、光斑、能量、尺寸、厚度等约束 |
| 自动优化 | 执行分阶段优化 |
| 指标评价 | 自动测量 EFL、F/# 或 NA、FOV、Spot、Encircled Energy、畸变/扫描线性度、相对照度等 |
| 多方案对比 | 对候选设计进行打分排序 |
| 报告输出 | 输出 Zemax 文件、指标表、图表、日志和复盘报告 |

### 4.2 MVP 暂不包含

| 暂不包含内容 | 原因 |
|---|---|
| 完全从零发明镜头结构 | 难度高，早期成功率不稳定 |
| 自动增删镜片 | 涉及复杂结构重构 |
| 完整杂散光分析 | 可作为后续扩展模块 |
| 完整公差优化 | 可作为后续扩展模块 |
| 扫描机构联动仿真 | 需要先确认系统形态 |
| 多物理场热结构耦合 | 后续扩展 |

---

## 5. 总体架构

系统建议分为六层：

```text
第一层：需求输入层
第二层：经验库 / 初始结构库
第三层：AI 策略规划层
第四层：Zemax 自动化执行器
第五层：指标评价与验收层
第六层：实验复盘与经验写回层
```

### 5.1 需求输入层

负责将自然语言、表格或 JSON 需求转换为标准设计任务。

示例：

```json
{
  "lens_type": "lidar_receiver",
  "wavelength_nm": 905,
  "fov_deg": 30,
  "efl_mm": 12,
  "f_number": 1.8,
  "detector_size_mm": 1.0,
  "spot_limit_um": 50,
  "encircled_energy_target": 0.8,
  "encircled_energy_radius_um": 25,
  "ttl_max_mm": 30,
  "material_constraint": "nir_transmissive"
}
```

### 5.2 经验库 / 初始结构库

存储历史激光雷达镜头案例、近红外成像镜头、扫描镜头、Zemax 实测参数、优化成功率、失败案例和策略经验。

### 5.3 AI 策略规划层

负责决定下一步策略，例如选择初始结构、是否缩放、是否调整视场、是否放宽光斑约束、是否切换能量集中度目标、是否释放非球面变量、是否回退。

### 5.4 Zemax 自动化执行器

负责真实操作 Zemax，包括连接、加载、新建、设置、优化、分析、保存和导出。

### 5.5 指标评价与验收层

负责读取 Zemax 分析结果，并判断设计是否达标。

激光雷达镜头核心指标包括：EFL、F/# 或 NA、FOV、工作波长、Spot RMS、几何光斑、Encircled Energy、探测器面能量覆盖、畸变或扫描线性度、相对照度、透过率相关材料检查、厚度、边缘厚度、空气间隔和全视场可追迹状态。

### 5.6 实验复盘与经验写回层

每次设计任务完成后，记录输入、初始结构、优化策略、操作数、优化前后指标、失败原因和后续建议，并写回经验库。

---

## 6. 已确认信息与后续输入

当前已确认：

1. 第一阶段先做接收端激光雷达镜头，后续再做发射端；
2. 接收端以面阵接收为主，探测器优先按 SPAD，其他镜头可支持 CMOS；
3. 激光雷达镜头通常按 F-theta 相关镜头考虑，需要关注扫描线性；
4. 工作波长优先支持 905±15nm 或 940±15nm；
5. FOV 由你提供全角，系统内部负责转换为 Zemax 需要的半视场；
6. 每次设计的焦距、F/#、NA、优化目标和机械约束由当次任务单独输入；
7. 材料组合包括全玻璃、玻塑混合、玻璃+玻璃非球面；
8. 如果设计中包含塑料镜片，需要考虑热分析；
9. 你会后续提供初始结构、已完成镜头、专利或规格资料。

后续开发过程中，如果遇到指标不明确、策略分歧或工程取舍，我会及时让你确认。

---

## 7. 第一阶段成功标准

| 指标 | 标准 |
|---|---|
| 自动连接 Zemax | 能稳定连接并执行基础操作 |
| 自动加载结构 | 能加载候选设计并回读参数 |
| 自动设置系统 | 能设置波长、视场、孔径、探测器面并验证 |
| 自动构建 Merit Function | 能生成并导出操作数表 |
| 自动优化 | 能完成至少一轮端到端优化 |
| 自动评价 | 能输出激光雷达核心指标和达标判断 |
| 自动报告 | 能生成设计结果报告 |
| 可复盘 | 每轮实验有完整日志、指标和策略记录 |

---

## 8. 推荐文档结构

```text
00_基于Zemax的自动光学设计AI框架总体方案.md
01_项目需求与范围定义文档.md
02_系统总体架构设计文档.md
03_Zemax_ZOS_API自动化执行器设计文档.md
04_指标评价与优化策略文档.md
05_经验库_实验系统与AI闭环文档.md
```

---

## 9. 总结

本项目第一阶段应聚焦激光雷达镜头，避免一开始做“大而全”的通用光学设计 AI。正确路径是：

```text
稳定 Zemax 自动化执行器
→ 建立激光雷达指标评价系统
→ 建立初始结构库和经验库
→ 加入 AI 策略规划
→ 批量实验形成自学习闭环
```

最重要的工程原则是：

1. AI 不直接乱改 Zemax；
2. 所有底层操作必须工具化；
3. 所有写入必须回读验证；
4. 所有优化必须有中间检查点；
5. 所有失败必须写入经验库；

