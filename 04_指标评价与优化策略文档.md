# 指标评价与优化策略文档

## 1. 文档目的

本文档定义激光雷达镜头自动设计系统如何评价一个设计是否达标，以及在不同设计状态下采用什么优化策略。

评价系统负责回答：

> 当前设计是否满足激光雷达需求？下一步应该继续优化、回退，还是换初始结构？

---

## 2. 核心评价指标

## 2.1 系统参数

| 指标 | 说明 | 推荐单位 |
|---|---|---|
| Wavelength | 工作波长 | nm |
| EFL | 有效焦距 | mm |
| F/# | 光圈，接收端常用 | 无量纲 |
| NA | 数值孔径，发射或接收均可能使用 | 无量纲 |
| FOV | 视场角或扫描角 | degree |
| BFL | 后焦或探测器安装距离 | mm |
| TTL | 总长 | mm |
| Max Diameter | 最大外径 | mm |

---

## 2.2 激光雷达核心性能指标

| 指标 | 说明 | 推荐单位 |
|---|---|---|
| Spot RMS | 点列图 RMS 半径 | μm |
| Spot Max / GEO Radius | 最大几何光斑 | μm |
| Encircled Energy | 指定半径内能量比例 | ratio 或 % |
| Detector Coverage | 光斑与探测器有效面积匹配程度 | pass/fail 或 ratio |
| Relative Illumination | 边缘相对照度或边缘能量 | ratio 或 % |
| Distortion | 畸变，接收成像场景关注 | % |
| Scan Linearity | 扫描线性度，F-theta 场景关注 | % 或 mm |
| Chief Ray Angle | 主光线角度，探测器耦合相关 | degree |
| Throughput Risk | 材料透过率风险 | pass/fail |

---

## 2.3 制造与工程约束

| 指标 | 说明 | 推荐单位 |
|---|---|---|
| Center Thickness | 中心厚度 | mm |
| Edge Thickness | 边缘厚度 | mm |
| Air Gap | 空气间隔 | mm |
| Glass Availability | 材料可获得性 | pass/fail |
| NIR Material Suitability | 近红外材料适用性 | pass/fail |
| Material Scheme | 全玻璃、玻塑混合、玻璃+玻璃非球面 | enum |
| Thermal Risk | 塑料镜片或混合材料的热稳定风险 | pass/fail |
| Ray Trace | 光线是否可追迹 | pass/fail |

---

## 3. 单位规范

所有指标必须经过统一单位转换层，不允许直接使用未确认的 API 原始值进入报告。

| 指标 | 报告单位 | 注意事项 |
|---|---|---|
| Wavelength | nm | Zemax 内部可能以 μm 显示 |
| EFL | mm | 与 Prescription Data 对照 |
| FOV | degree | 必须确认全角还是半角 |
| BFL | mm | 明确最后一面到探测器面距离 |
| TTL | mm | 明确计算起止面 |
| Spot RMS | μm | 注意 API 可能返回 mm |
| Encircled Energy | ratio 或 % | 报告中必须明确表达方式 |
| Distortion | % | 注意小数和百分比区分 |
| Scan Linearity | % 或 mm | 需与扫描系统定义一致 |

---

## 4. 设计达标判据

第一阶段建议使用如下标准，具体数值由你确认。

| 指标 | 达标条件 |
|---|---|
| Wavelength | 与目标波长一致 |
| EFL | 目标 ±1%，或满足 FOV/探测器匹配 |
| F/# 或 NA | 满足接收通光量或发射准直要求 |
| FOV | 覆盖目标视场或扫描角 |
| Spot | 小于探测器尺寸、像元尺寸或指定阈值 |
| Encircled Energy | 指定半径内能量比例达标 |
| Relative Illumination | 边缘视场不低于下限 |
| Distortion / Scan Linearity | 满足系统标定或扫描要求 |
| Ray Trace | 全视场、全波长可追迹 |
| TTL / 外径 | 满足机械包络 |
| BFL | 满足探测器安装空间 |
| Center / Edge Thickness | 大于制造下限 |
| Air Gap | 大于最小空气间隔 |
| Material | 近红外透过率和可制造性满足要求 |

---

## 5. 综合评分方法

建议设计一个激光雷达镜头综合评分。

```text
总分 = 光斑与能量得分 × 40%
     + 视场与探测器匹配得分 × 25%
     + 工程约束得分 × 20%
     + 制造与材料得分 × 15%
```

第一阶段先按接收端 / 面阵接收 / F-theta 相关镜头评分。后续扩展到发射端准直镜头时，评分权重需改为以发散角、准直质量和准直效率为主。

---

## 6. 优化策略

## 6.1 初始结构验证策略

初始结构加载后，先不要立即大规模优化。应先验证：

1. 文件可加载；
2. 工作波长正确；
3. 全视场可追迹；
4. 表面数量正确；
5. 视场和孔径正确；
6. 探测器面或像面位置正确；
7. 初始光斑没有明显崩溃。

如果初始结构不可追迹，应直接换候选结构，不要盲目优化。

---

## 6.2 基础优化策略

适用于初始可追迹但指标未达标的阶段。

原则：

1. 使用基础像质目标；
2. 优先控制焦距、视场、像面位置和光斑；
3. 释放曲率和厚度等基础变量；
4. 不立即释放全部非球面变量；
5. 每轮优化后保存检查点。

---

## 6.3 专家优化基线流程：Zemax 优化向导优先

这是第一阶段自动优化器需要优先模仿的人工工程师流程。核心思路是：**先让镜头形成稳定、可制造、基础像质较好的状态，再逐步加入具体性能指标**，不要一开始就堆大量像差操作数。

适用前提：镜片数量已经基本确定，并且已有可追迹的初始结构。

推荐流程：

```text
加载初始结构
→ 初始结构和追迹检查
→ 先不更换材料
→ 使用 Zemax 优化向导生成默认评价函数
→ 优先选择 Spot Diagram / 点列图目标做基础优化
→ 根据结果交替使用 MTF 或 Wavefront 目标
→ 加入制造型评价函数
→ 加入焦距或 F/# 约束
→ 释放合理变量并加入厚度、空气间隔等制造约束
→ 得到较好基础结果后，再加入具体性能指标
→ 若仍无法达标，再考虑材料高低折组合变化
→ 重新优化、评价和复盘
```

关键原则：

1. 初始阶段先不改材料，避免材料自由度过早放大搜索空间；
2. 默认 Merit Function 以 Zemax 优化向导为基础，而不是从零手工堆像差操作数；
3. 初始优化优先使用点列图目标，必要时交替使用 MTF 或波前目标；
4. 不建议一开始直接控制具体像差操作数，容易让优化方向被局部像差项牵制；
5. 默认评价函数通常由“优化向导目标 + 镜片制造型评价函数 + 焦距或 F/# 约束”组成；
6. 曲率、厚度等可设为变量，但中心厚度、边缘厚度、空气间隔必须有制造约束；
7. 只有在基础像质已经较好后，才加入 Encircled Energy、扫描线性、相对照度等具体性能指标；
8. 如果指标仍无法达标，再考虑换材料或做高低折材料组合调整；
9. 材料变化后必须重新进行基础优化，不应直接沿用旧权重；
10. 所有操作数和权重最终以用户提供或人工确认为准。

---

## 6.4 光斑控制策略

适用于接收端或面阵接收激光雷达镜头。

目标：

1. 控制中心和边缘视场 Spot RMS；
2. 控制最大几何光斑；
3. 使光斑与探测器有效面积匹配；
4. 避免边缘视场光斑明显扩散。

策略：

```text
先优化中心和中间视场
→ 加入边缘视场
→ 平衡全视场光斑
→ 约束探测器面能量覆盖
```

---

## 6.5 Encircled Energy 优化策略

激光雷达系统往往不只看 RMS 光斑，还需要看指定探测器半径内的能量比例。

策略：

1. 先保证全视场可追迹；
2. 再控制几何光斑；
3. 然后加入 Encircled Energy 目标；
4. 不要让能量集中度目标过早主导优化；
5. 每次优化后检查边缘视场能量是否下降。

---

## 6.6 F/# 或 NA 策略

接收端通常关注通光量，发射端通常关注准直和发散角。

原则：

1. 不要盲目追求极小 F/#；
2. F/# 或 NA 必须与探测器、视场和机械口径共同平衡；
3. 如果 F/# 推进导致边缘视场崩溃，应回退；
4. 如果发射端以准直为目标，应转为使用发散角和远场光斑指标。

---

## 6.7 非球面变量释放策略

推荐顺序：

```text
阶段 1：不释放非球面
阶段 2：只释放圆锥系数
阶段 3：释放低阶非球面项
阶段 4：必要时释放更高阶项
```

原则：

1. 不要一开始释放所有高阶项；
2. 每次只增加少量变量；
3. 圆锥系数通常比高阶项更稳定；
4. 如果边缘视场崩溃，立即回退；
5. 每次释放变量后导出变量表。

---

## 6.8 畸变与扫描线性策略

对于普通接收镜头，畸变可能不是第一优先级；对于扫描 F-theta 镜头，扫描线性度可能是核心指标。

策略：

1. 接收镜头：畸变作为可标定误差，通常使用范围约束；
2. 扫描 F-theta 镜头：优先约束扫描线性和像高关系；
3. 不建议过早追求畸变为 0；
4. 畸变权重过高可能牺牲光斑和能量集中度。

---

## 6.9 失败回退策略

| 失败情况 | 处理方式 |
|---|---|
| 光线不可追迹 | 回退到上一检查点 |
| 光斑明显变大 | 降低变量自由度或回退策略 |
| Encircled Energy 下降 | 调整权重或先恢复光斑控制 |
| 边缘照度过低 | 增强边缘视场权重或调整口径 |
| 厚度为负 | 加强厚度约束 |
| 空气间隔异常 | 加强最小空气间隔约束 |
| 畸变或扫描线性异常 | 检查视场定义和操作数 |
| 优化无改善 | 更换初始结构 |
| Zemax 断连 | 重启并加载最近检查点 |

---

## 7. 推荐优化流程

```text
加载候选结构
→ 设置工作波长
→ 设置视场和探测器面
→ 基线指标测量
→ 判断是否可追迹
→ 基础变量释放
→ 光斑控制
→ Encircled Energy 控制
→ 边缘照度 / 视场平衡
→ 机械包络和厚度约束
→ 全指标验收
→ 输出报告
```

---

## 8. 已确认信息与单次任务确认项

当前已确认：

1. 第一阶段按接收端 / 面阵接收镜头评价；
2. 初始阶段不以 MTF 为唯一目标，而是优先用点列图基础优化，并可交替使用 MTF 或波前；
3. FOV 由用户提供全角；
4. 需要关注 F-theta 相关的扫描线性；
5. 发射端放在接收端之后扩展；
6. 使用塑料镜片时需要考虑热分析；
7. 具体评价操作数由用户提供或确认。

每次具体设计时需要确认：

1. Spot 阈值；
2. Encircled Energy 的统计半径和目标比例；
3. 扫描线性或畸变限制；
4. 相对照度或边缘能量要求；
5. 材料方案和热稳定要求；
6. 体积、成本和制造约束优先级。

---

## 9. 总结

激光雷达镜头评价和优化的核心原则是：

1. 先保证工作波长、视场和追迹正确；
2. 先控制光斑，再控制能量集中度；
3. 接收端优先考虑探测器匹配和能量收集；
4. 发射端优先考虑准直质量和发散角；
5. 扫描系统优先考虑扫描线性；
6. 每一步都要保存、测量、复盘。
