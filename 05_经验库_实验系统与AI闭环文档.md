# 经验库、实验系统与 AI 闭环文档

## 1. 文档目的

本文档定义激光雷达镜头自动设计系统如何积累经验、如何设计批量实验、如何记录失败案例，以及如何让 AI 从历史任务中学习，形成持续改进的闭环。

系统真正变强的关键，不只是能优化出一个设计，而是每次成功和失败都能变成后续可复用的经验。

---

## 2. 经验库组成

经验库建议分为五类：

```text
1. 初始结构库
2. 操作数知识库
3. 优化策略库
4. 失败案例库
5. 项目实验记录库
```

---

## 3. 初始结构库

初始结构库用于存储可作为激光雷达镜头设计起点的案例。

### 3.1 数据来源

1. 用户提供的初始结构或已经完成的 Zemax 镜头；
2. 激光雷达专利镜头；
3. 近红外接收镜头；
4. 面阵接收镜头；
5. 扫描 F-theta 镜头；
6. 发射准直镜头；
7. 历史项目文件；
8. Zemax 示例文件；
9. 公开论文或公开案例；
10. 人工整理的高质量设计。

### 3.2 字段设计

| 字段 | 说明 |
|---|---|
| lens_id | 镜头编号 |
| lens_type | 接收端/发射端/收发一体/扫描 F-theta/面阵接收 |
| source | 来源，专利/项目/示例等 |
| zemax_file | Zemax 文件路径 |
| wavelength_nm | 工作波长 |
| efl_mm | Zemax 实测焦距 |
| f_number | Zemax 实测 F/# |
| numerical_aperture | 数值孔径 |
| fov_deg | 视场角或扫描角 |
| detector_size_mm | 探测器尺寸 |
| spot_rms_um | 实测 RMS 光斑 |
| spot_max_um | 实测最大几何光斑 |
| encircled_energy | 指定半径内能量比例 |
| relative_illumination | 边缘相对照度 |
| bfl_mm | 后焦或探测器安装距离 |
| ttl_mm | 总长 |
| max_outer_diameter_mm | 最大外径 |
| lens_count | 镜片数量 |
| asphere_count | 非球面数量 |
| material_summary | 材料摘要 |
| material_scheme | 全玻璃、玻塑混合、玻璃+玻璃非球面 |
| plastic_thermal_analysis_required | 是否需要塑料热分析 |
| thermal_risk | 热稳定性风险 |
| nir_material_ok | 近红外材料适用性 |
| verified_in_zemax | 是否经过 Zemax 实测 |
| ray_trace_pass | 是否全视场可追迹 |
| success_rate | 历史优化成功率 |
| notes | 备注 |

### 3.3 关键原则

所有入库参数应优先采用 Zemax 实测结果，而不是离线估算结果。

原因是离线估算容易出现焦距、视场、光斑、能量统计、单位等偏差，可能导致 AI 选择错误的起点。

---

## 4. 操作数知识库

操作数知识库记录 Merit Function 操作数的使用方式、单位、适用场景和常见错误。

### 4.1 字段设计

| 字段 | 说明 |
|---|---|
| operand_name | 操作数名称 |
| purpose | 用途 |
| unit | 单位 |
| parameters | 参数含义 |
| applicable_scene | 适用场景 |
| common_mistakes | 常见错误 |
| example | 示例 |
| verification_method | 验证方法 |
| risk_level | 风险等级 |

### 4.2 示例

```text
操作数：EFFL
用途：约束有效焦距
单位：mm
适用场景：接收端成像或聚焦系统
常见错误：权重过大导致牺牲光斑或能量集中度
验证方法：与 Zemax Prescription Data 对照
```

```text
操作数：Spot / RMS 类操作数
用途：控制探测器面光斑尺寸
单位：通常需转换为 μm
适用场景：接收端、面阵接收镜头
常见错误：只优化中心视场，忽略边缘视场
验证方法：与 Spot Diagram 对照
```

```text
操作数：Encircled Energy 类目标
用途：控制指定半径内能量比例
单位：ratio 或 %
适用场景：激光雷达接收效率优化
常见错误：半径单位混乱，或过早加入导致优化发散
验证方法：与 Encircled Energy 分析结果对照
```

---

## 5. 优化策略库

优化策略库记录在不同激光雷达镜头场景下应采用的优化方法。

### 5.1 字段设计

| 字段 | 说明 |
|---|---|
| strategy_id | 策略编号 |
| strategy_name | 策略名称 |
| applicable_lens_type | 适用镜头类型 |
| trigger_condition | 触发条件 |
| actions | 执行动作 |
| success_condition | 成功条件 |
| failure_condition | 失败条件 |
| rollback_method | 回退方法 |
| historical_success_rate | 历史成功率 |
| notes | 备注 |

### 5.2 示例策略：Zemax 优化向导基础优化

```text
策略名称：Zemax 优化向导基础优化
适用镜头：已有初始结构且镜片数量基本确定的激光雷达接收镜头
触发条件：初始结构可追迹，但尚未达到较好基础像质
动作：先不改材料，调用 Zemax 优化向导生成默认评价函数，优先用点列图目标优化，必要时交替使用 MTF 或 Wavefront，并叠加制造约束、焦距或 F/# 约束
成功条件：全视场可追迹，基础光斑明显改善，厚度和空气间隔满足制造约束
失败处理：回退检查点，减少变量或调整优化向导目标；仍不达标时再考虑材料高低折组合变化
说明：该策略为第一阶段自动优化器优先模仿的人工专家流程，正式写入经验库前需要人工确认。
```

### 5.3 示例策略：接收端光斑控制

```text
策略名称：接收端光斑控制
适用镜头：激光雷达接收镜头、面阵接收镜头
触发条件：全视场可追迹，但光斑超过探测器或像元限制
动作：先控制中心和中间视场 Spot，再加入边缘视场权重
成功条件：全视场光斑小于阈值
失败处理：降低变量自由度，回退到上一检查点，或更换初始结构
```

### 5.4 示例策略：Encircled Energy 控制

```text
策略名称：能量集中度控制
适用镜头：激光雷达接收镜头
触发条件：光斑基本达标，但指定探测器半径内能量不足
动作：加入 Encircled Energy 目标，并平衡边缘视场权重
成功条件：指定半径内能量比例达标
失败处理：降低能量权重，先恢复光斑控制
```

### 5.5 示例策略：非球面渐进释放

```text
策略名称：非球面渐进释放
适用镜头：含非球面镜头
触发条件：基础变量已无法继续改善光斑或能量集中度
动作：先释放圆锥系数，再逐步释放低阶非球面项
成功条件：光斑或能量改善且边缘视场未崩溃
失败处理：回退高阶项，只保留圆锥系数
```

---

## 6. 失败案例库

失败案例库非常重要。系统需要把失败变成可复用经验。

### 6.1 字段设计

| 字段 | 说明 |
|---|---|
| task_id | 任务编号 |
| lens_id | 初始结构编号 |
| failure_stage | 失败阶段 |
| failure_type | 失败类型 |
| zemax_message | Zemax 报错或异常 |
| before_metrics | 失败前指标 |
| after_metrics | 失败后指标 |
| merit_function_snapshot | 操作数快照 |
| surface_snapshot | 表面参数快照 |
| variable_snapshot | 变量快照 |
| root_cause | 根因分析 |
| solution | 解决方法 |
| avoid_rule | 后续规避规则 |

### 6.2 典型失败类型

1. 光线不可追迹；
2. 表面添加失败；
3. 视场设置失败；
4. 波长设置失败；
5. 探测器面设置错误；
6. 操作数引用错误；
7. 单位理解错误；
8. 光斑发散；
9. Encircled Energy 下降；
10. 边缘视场崩溃；
11. 边缘照度过低；
12. 厚度为负；
13. 空气间隔异常；
14. 非球面变量过多导致不稳定；
15. 材料在近红外波段不适用；
16. Zemax 长时间运行断连。

---

## 7. 项目实验记录库

每次优化任务都应生成结构化实验记录。

### 7.1 实验记录示例

```json
{
  "task_id": "lidar_receiver_001",
  "lens_id": "candidate_008",
  "strategy": "spot_energy_balance",
  "stage": "energy_control",
  "before_metrics": {
    "spot_rms_um": 25.0,
    "spot_max_um": 62.0,
    "encircled_energy_25um": 0.68
  },
  "after_metrics": {
    "spot_rms_um": 18.5,
    "spot_max_um": 45.0,
    "encircled_energy_25um": 0.83
  },
  "pass": true,
  "failure_reason": null,
  "next_action": "package_constraint_check"
}
```

### 7.2 每轮必须记录

1. 任务输入；
2. 初始结构；
3. 当前阶段；
4. 当前策略；
5. 当前变量；
6. Merit Function 快照；
7. 表面参数快照；
8. 优化前指标；
9. 优化后指标；
10. 是否达标；
11. 失败原因；
12. 下一步建议。

---

## 8. 批量实验系统

为了形成自学习能力，需要支持批量实验。

实验维度可以包括：

```text
初始结构 A/B/C
× 工作波长 905/940/1550
× FOV 配置 1/2/3
× F/# 或 NA 配置 1/2/3
× 变量释放策略 1/2/3
× Merit Function 模板 1/2/3
× Spot 权重 1/2/3
× Encircled Energy 权重 1/2/3
```

### 8.1 批量实验目标

1. 找出哪些初始结构更容易成功；
2. 比较不同变量释放策略的稳定性；
3. 比较不同 Merit Function 模板的效果；
4. 发现哪些约束最容易导致发散；
5. 总结光斑和能量目标的权重关系；
6. 形成可复用的策略规则；
7. 给 AI 决策提供数据依据。

---

## 9. AI 决策闭环

推荐闭环如下：

```text
用户需求
→ AI 解析任务
→ 经验库筛选候选结构
→ AI 生成优化计划
→ Zemax 执行器执行
→ 指标评价模块打分
→ AI 分析结果
→ 决定继续优化 / 回退 / 换结构
→ 输出报告
→ 写回经验库
```

---

## 10. AI 职责边界

### 10.1 AI 应该负责

1. 需求理解；
2. 策略选择；
3. 候选结构推荐；
4. 失败原因分析；
5. 实验总结；
6. 报告生成；
7. 经验归纳。

### 10.2 AI 不应该直接负责

1. 绕过工具函数修改 Zemax；
2. 不经验证地相信 API 返回值；
3. 没有日志地修改 Merit Function；
4. 没有检查点地释放大量变量；
5. 没有回退机制地连续优化；
6. 将未验证经验写入正式知识库。

---

## 11. 已确认信息与后续确认项

当前已确认：

1. 初始结构库优先收集接收端、面阵接收、F-theta 相关案例；
2. 后续再补充发射端案例；
3. 用户会提供初始结构、已完成 Zemax 镜头或相关资料。

当前决策：

1. 成功经验先人工确认，再写入正式经验库；
2. 用户提供的 Zemax 镜头文件可直接入库，用户会说明哪些属于激光雷达镜头；
3. 第一阶段先做单任务闭环，批量实验作为后续扩展。

说明：批量实验是指系统一次性对多个初始结构、多个 FOV、多个 F/# 或多组优化权重进行自动组合测试，用来比较哪种结构和策略更容易成功。第一阶段可以先不做。

---

## 12. 总结

经验库和 AI 闭环的核心目标是让系统越用越强。

最重要原则：

```text
成功案例沉淀为策略；
失败案例沉淀为规避规则；
实验结果沉淀为数据；
AI 决策必须基于可验证经验。
```
