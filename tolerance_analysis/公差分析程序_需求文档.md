# Zemax 公差分析自动化程序需求文档

> 面向维护者：记录当前功能规格、设计边界和后续规划。  
> 使用方法见 `公差分析程序_使用说明.md`；开发和测试记录见 `公差分析工具_开发进度与测试记录.md`。

---

## 1. 项目目标

将 Zemax 公差分析流程固化为配置驱动程序：

```text
选择 zmx → 生成/复用 TDE/MFE/TSC → 运行脚本式 Monte Carlo → 保存 ZTD → 导出统计 Excel
```

核心原则：

1. 不修改用户原始 zmx，只操作工作副本；
2. 普通用户可以不写 Excel；
3. 专家用户仍可用高级 Excel 完全控制；
4. 每次运行结果可追溯；
5. Zemax API 细节集中在代码和 skill 中维护，文档只保留规格。

---

## 2. 当前已实现范围

### 2.1 运行模式

| 模式 | 状态 | 说明 |
|---|---|---|
| 普通标准模板模式 | 已实现 | 内置 RX/TX、标准分析/完整视场分析 |
| 使用 Zemax 当前设置模式 | 已实现 | 保留当前 TDE，读取当前 MFE 生成 REPORT/TSC |
| 高级 Excel 模式 | 已实现 | Excel 配置 TDE、MFE、REPORT、运行参数 |
| 独立 ZTD 分析 | 已实现 | 不重新运行 Zemax，读取已有 ZTD 导出统计 |

### 2.2 程序入口

| 入口 | 状态 | 说明 |
|---|---|---|
| `gui.py` | 已实现 | 日常推荐入口，PySide6 GUI |
| `tol_run.py` | 已实现 | 命令行入口 |

### 2.3 连接方式

| 连接方式 | 状态 | 说明 |
|---|---|---|
| Standalone | 已实现，推荐 | 程序后台启动/释放 Zemax |
| Interactive Extension | 已实现 | 连接用户已打开并等待交互扩展的 OpticStudio |

---

## 3. 功能规格

### 3.1 普通标准模板模式

面向普通使用者，目标流程：

```text
选择镜头文件 → 选择产品/模板/次数 → 开始分析 → 获取统计 Excel
```

当前支持：

| 项 | 规格 |
|---|---|
| 产品类型 | `RX` / `TX` |
| 模板 | `标准分析` / `完整视场分析` |
| 公差等级 | 宽松 / 标准 / 严格 |
| 视场映射 | 默认开启，输出 `field_mapping.txt` |
| 主波长 | 运行期自动使用 Zemax 主波长 |
| 结束面 | 默认像面前一面 |
| 标准分析面范围 | `全部面=是` 保持默认全范围；`全部面=否` 时直接读 zmx 文本按 TX/RX 与滤光片裁剪，只保留镜头面 |
| 后焦补偿面 | RX 可按补偿模式自动/手动设置；TX 固定不补偿 |
| 临时配置 | 生成 UUID 文件名，避免覆盖用户文件 |

当前模板特性：

- RX：包含 Spot / GENC / MTF；
- TX：不含 MTF，固定 `补偿器模式=无`，不写 TSC 优化，不加 TDE `COMP`；
- 标准模板动态追加：
  - 中心指向偏移 `POINTING_DY_F0_mm` / `POINTING_DX_F0_mm`；
  - 焦距偏移百分比 `EFL_DELTA_PCT`；
  - 视场角 `FOV_Y_deg`；
  - 多视场指向角偏差 `POINTING_ANGLE_F*_deg`。

### 3.2 使用 Zemax 当前设置模式

面向已经在 zmx 中配置好 TDE/MFE 的用户。

当前策略：

1. 打开工作副本；
2. 不清空 TDE；
3. 不重建 MFE；
4. 读取当前 MFE 有效行；
5. 自动生成 REPORT/TSC；
6. 运行 Monte Carlo；
7. 导出统计 Excel。

补偿器规则：

- 若请求补偿，但当前 TDE 中没有 `COMP`，程序会关闭优化并记录日志；
- COMP 统计按 TDE 顺序定位，不靠表头模糊匹配。

### 3.3 高级 Excel 模式

面向项目专家，保留完整自定义能力。

Excel sheet：

| Sheet | 规格 |
|---|---|
| `输入_公差向导` | 向导式批量 TDE |
| `输入_公差明细` | 逐行追加 TDE，可粘贴 Zemax TDE 数据行 |
| `输入_评价函数` | MFE 操作数 |
| `输入_REPORT` | TSC `REPORT` 分项 |
| `输入_运行参数` | MC、补偿、分布、保存、统计输出 |

视场映射：

- 标准模板默认启用；
- 高级 Excel 可通过运行参数启用；
- 当前只支持 X/Y 主轴视场；
- 插入视场只作用于工作副本；
- 输出 `field_mapping.txt` 和 `run_config.json` 记录映射摘要。

### 3.4 ZTD 统计

统计 Excel 固定输出：

- 名义值；
- Cpk1.33 下限 / 上限；
- 最大值、最小值、平均值、标准差、中位数；
- MC 原始样本；
- TSC REPORT 分项；
- TDE COMP 项。

列名策略：

1. 优先从同名 `.TSC` 解析 `REPORT "标签" 行号`；
2. COMP 按 `run_config.json` 中的 TDE 元数据定位；
3. 无法识别时保留 `colN`，不中断导出。

### 3.5 敏感度排序（第一版实现）

目标：在给定公差下，识别每个 REPORT 指标最敏感的公差项，辅助判断图纸严控项。

原则：

1. 优先复用 Monte Carlo 生成的同一份 `.ZTD`，不重复运行 Zemax；
2. 仅在用户选择“只做敏感度排序”时，才尝试 `NumberOfRuns=0` 的轻量运行；
3. 第一版不做跨类型综合排序，不把 MTF、SPT、指向、焦距等不同物理指标强行合并；
4. 每个 REPORT 指标单独排序，只保留 Top 20；
5. 敏感度结果写入统计 Excel 的独立 sheet。

排序口径：

```text
单指标敏感度 = abs(ZTD 中该公差项对该 REPORT 的预计变化量)
```

输出位置：统计 Excel 内追加敏感度 sheet。

计划 sheet：

| Sheet | 内容 |
|---|---|
| `敏感度Top20` | 每个 REPORT 指标下，按绝对值排序的前 20 个 TDE 公差项 |
| `敏感度诊断` | ZTD、数据源、排序规则、SensitivityData 行列数、TDE/REPORT 匹配情况、失败原因 |

`敏感度Top20` 字段：

| 字段 | 说明 |
|---|---|
| REPORT指标 | TSC `REPORT` 标签 |
| 排名 | 当前 REPORT 内排名 |
| 公差项 | TDE 操作数及说明 |
| 名义值 | 当前 REPORT 指标的标称值 |
| 公差类型 | 半径/厚度/偏心/倾斜等 |
| 表面/元件 | 从 TDE Param 或 Comment 反推 |
| 当前公差 | Min/Max 或等效公差幅值 |
| 值 | ZTD SensitivityData 给出的预计变化量，保留正负号 |
| 绝对值 | `值` 的绝对值，排序依据 |
| 风险方向 | 根据 REPORT 方向标记“不利/有利/需判断/未配置” |

后续扩展：可在 REPORT 配置中增加“指标组”，只在同类指标组内做组排序，例如 MTF 组、SPT 组、指向组；不做全指标混合总排名。

代码实现状态：

1. 已新增 `toltool/sensitivity_reader.py`：读取 `ToleranceDataViewer.SensitivityData`，匹配 TDE 元数据与 REPORT 标签，并写入统计 Excel 的敏感度 sheet；
2. 已扩展 `pipeline.export_sensitivity()`：统计 Excel 成功后，尝试追加敏感度 sheet；失败只写日志，不影响原统计 Excel；
3. 已扩展 `gui.py` / `tol_run.py` 日志展示统计 Excel 路径；
4. 后续再根据实测 ZTD 数据结构修正 SensitivityData 行列匹配；
5. 后续再考虑“只做敏感度排序”模式和 GUI 开关。

---

## 4. 输出规格

每次运行生成独立结果目录：

```text
公差分析_<镜头名>_YYYYMMDD_HHMMSS
```

| 产物 | 规格 |
|---|---|
| 工作副本 `.zmx` | 始终生成，不改原文件 |
| `used_excel.xlsx` | 本次实际运行配置快照 |
| `run_config.json` | 运行参数、视场映射、REPORT/TDE 元数据 |
| `run.log` | 全流程日志 |
| `.ZTD` | Zemax 公差数据 |
| `_统计.xlsx` | 统计结果 |
| 统计 Excel 内 `敏感度Top20` / `敏感度诊断` | 从同一 ZTD 读取 SensitivityData，按每个 REPORT 指标输出 Top 20 |
| `field_mapping.txt` | 启用视场映射时生成 |
| `.TSC` | 保存到 Zemax DataDir 的 `Tolerance` 目录 |
| `.MF` | 保存到 Zemax DataDir 的 `MeritFunction` 目录 |
| Worst/Best Case | 由 Zemax API 开关统一控制 |

---

## 5. 模块划分

| 模块 | 职责 |
|---|---|
| `gui.py` | GUI、线程调度、日志展示 |
| `tol_run.py` | 命令行入口 |
| `toltool/pipeline.py` | 单一主流程：准备会话 + 运行蒙卡 |
| `toltool/zos_connect.py` | 连接 Zemax、另存工作副本、读取镜头信息 |
| `toltool/excel_io.py` | Excel 模板生成和配置读取 |
| `toltool/standard_templates.py` | 标准模板定义 |
| `toltool/current_settings.py` | 读取当前 TDE/MFE |
| `toltool/field_mapping.py` | 视场映射与插入工作副本 |
| `toltool/tde_builder.py` | 写 TDE |
| `toltool/mfe_builder.py` | 写/保存 MFE |
| `toltool/tsc_builder.py` | 生成 TSC |
| `toltool/tol_runner.py` | 配置并运行 OpenTolerancing |
| `toltool/ztd_reader.py` | 读取 ZTD 并导出统计 Excel |
| `toltool/sensitivity_reader.py` | 读取 ZTD SensitivityData，生成单 REPORT Top 20 敏感度明细 |

主调用链：

```text
gui.py / tol_run.py
  → pipeline.prepare_session()
      → excel_io / current_settings / standard_templates
      → zos_connect
      → field_mapping
      → tde_builder
      → mfe_builder
      → tsc_builder
  → pipeline.run_montecarlo()
      → tol_runner
  → ztd_reader
```

---

## 6. 当前不做 / 暂缓

| 项 | 状态 |
|---|---|
| 反向求解模式 | 暂不做，当前只做脚本式 Monte Carlo |
| 跨类型综合敏感度总排名 | 暂不做，不将 MTF/SPT/指向等不同物理指标强行合并排序 |
| 自动 Word/PDF 报告 | 暂不做 |
| 分项直方图 | 预留，默认关闭 |
| 公差资料自动识别 | 低优先级规划 |
| 当前设置模式中手动选择 REPORT 行 | 后端保留，GUI 暂不开放 |
| 保留当前 TDE + 标准评价模板 | 未开发 |
| 斜向二维视场自动投影/插入 | 暂不支持 |
| Monte Carlo 逐次进度 | 已确认 Zemax 脚本式蒙卡不暴露，GUI 不做伪进度 |

---

## 7. 后续产品化规划

从已删除的“泛化需求文档”合并保留以下规划方向：

### 7.1 普通用户产品化

目标是让普通用户只需：

```text
选择镜头 → 选择标准模板 → 设置次数 → 开始分析 → 获取统计 Excel
```

重点改进：

- 模板由光学负责人定稿；
- GUI 文案更面向非开发者；
- 错误提示从 API 细节转为可操作建议；
- 输出目录只突出核心结果：统计 Excel、日志、ZTD。

### 7.2 模板维护

后续可增加模板维护能力：

- 产品线模板；
- 默认 MTF 频率；
- 默认公差等级；
- 默认 REPORT 项；
- 模板导入/导出。

### 7.3 公差资料识别

低优先级方向：

```text
结构公差资料 → 自动识别候选公差项 → 用户确认 → 写入 Excel 或 TDE
```

约束：识别结果必须人工确认，不能静默写入分析流程。

---

## 8. 关键约束索引

Zemax API 细节不在本文展开，维护时优先查看：

- skill：`zemax-tolerance-analysis`；
- [tde_builder.py](file:///e:/zemax-skills-advance_TOL/tolerance_analysis/toltool/tde_builder.py)；
- [pipeline.py](file:///e:/zemax-skills-advance_TOL/tolerance_analysis/toltool/pipeline.py)；
- [tsc_builder.py](file:///e:/zemax-skills-advance_TOL/tolerance_analysis/toltool/tsc_builder.py)；
- [tol_runner.py](file:///e:/zemax-skills-advance_TOL/tolerance_analysis/toltool/tol_runner.py)；
- [ztd_reader.py](file:///e:/zemax-skills-advance_TOL/tolerance_analysis/toltool/ztd_reader.py)。
