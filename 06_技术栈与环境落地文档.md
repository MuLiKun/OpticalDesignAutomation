# 技术栈与环境落地文档

## 1. 文档目的

本文档定义激光雷达镜头自动设计项目的技术栈、运行环境、连接方式、目录结构和开发起步步骤，保证项目从架构文档过渡到可执行代码时不会卡在环境层。

本项目复用工作区中已跑通的 Zemax 连接方案（参考 `tolerance_analysis/toltool/zos_connect.py`），不重新造轮子。

---

## 2. 技术栈

| 项目 | 选型 | 说明 |
|---|---|---|
| 光学软件 | Zemax OpticStudio | 需支持 ZOS-API |
| API | ZOS-API (.NET) | 通过 pythonnet 调用 |
| .NET 桥接 | pythonnet (clr) | 加载 ZOSAPI DLL |
| 开发语言 | Python 3.x | 与工作区现有 tol 工程一致 |
| 虚拟环境 | 工作区共享 venv `.venv\Scripts\python.exe` | 与 tol 工程共用，`.venv` 不入库 |
| 操作系统 | Windows | ZOS-API 仅支持 Windows |
| 数据格式 | JSON / Excel / Markdown | 任务输入、指标表、报告 |
| 版本管理 | Git | 远程见 project_rules |

技术栈与工作区 tol 工程保持一致，直接复用其连接方案和共享 venv，不引入新的语言或运行时。

### 关于是否使用其他技术栈

结论：不建议更换，继续使用 Python + ZOS-API(.NET) + pythonnet。

1. ZOS-API 官方仅提供 .NET / COM 接口，Python 通过 pythonnet 调用是成熟主流路线，换其他语言不会更简单；
2. 工作区 tol 工程已用同一套方案跑通，复用风险最低；
3. Python 的 AI、数据处理、绘图和后续经验库/自学习生态最完整；
4. 换 C# / MATLAB 只会与现有工具割裂，无实际收益。

需要注意的不是换语言，而是 pythonnet 的已知约束（tol 工程已验证）：

1. 枚举参数必须传枚举成员对象，不能传 int；
2. 单位不能假设，必须回读并与 GUI 核对；
3. 这些约束在执行器文档中作为强制规则执行。

需要在开发前由你或环境核实的版本：

1. Zemax OpticStudio 具体版本（例如 2023 R1）；
2. 是否有可用的 ZOS-API 授权（Premium / Professional 决定部分分析可用性）；
3. Python 版本（与 tol 工程共享 venv 保持一致）；
4. pythonnet 版本（与 tol 工程共享 venv 保持一致）。

---

## 3. ZOS-API 连接方式

### 3.1 DLL 定位

复用现有方案的定位顺序：

```text
环境变量 ZEMAX_ZOS_DIR
→ 项目 zemax_config.ini 中 zos_dir
→ 注册表 (HKCU / HKLM 下 Zemax / Ansys Zemax OpticStudio)
→ 盘符 + 常见安装路径模板 glob
```

判定目录有效的条件：同时包含

```text
ZOSAPI_NetHelper.dll
ZOSAPI_Interfaces.dll
ZOSAPI.dll
```

### 3.2 连接模式

| 模式 | 用途 | 第一阶段 |
|---|---|---|
| ConnectAsExtension | 连接已打开的 OpticStudio，可看 GUI | 默认，便于调试 |
| CreateNewApplication | 启动独立后台实例 | 后续批量/无人值守时用 |

第一阶段默认使用 **Interactive Extension**，方便一边跑一边在 GUI 中核对结果。

### 3.3 连接检查

连接成功后必须验证：

1. 是否取得 Application 对象；
2. 是否取得 PrimarySystem；
3. 系统是否可编辑；
4. 是否为顺序模式（第一阶段不涉及非序列）；
5. 版本是否满足要求。

---

## 4. 配置文件

沿用现有 `zemax_config.ini` 约定：

```ini
[zemax]
# Zemax OpticStudio 安装目录（含 ZOSAPI_NetHelper.dll）
zos_dir = C:\Program Files\Ansys Zemax OpticStudio 2023 R1.00
```

自动查找失败时提示用户手动填写，并写回配置文件。敏感/本机路径文件不入库。

---

## 5. 代码目录结构

第一阶段建议在本项目目录下建立代码骨架（暂不创建，待你确认后再建）：

```text
zemax-skills-opt_design/
├── docs/                     # 现有 6 份设计文档可后续归入
├── auto_optical_ai/
│   ├── config/               # 连接配置、默认参数
│   ├── zemax_api/            # ZOS-API 封装（连接、系统、表面、变量、优化、分析）
│   ├── requirements/         # 任务输入解析、校验
│   ├── merit_function/       # Merit Function 构建
│   ├── evaluator/            # 指标评价与单位换算
│   ├── strategy/             # 优化策略
│   ├── agent/                # AI 决策编排
│   ├── lens_library/         # 初始结构库 / 经验库
│   ├── experiments/          # 单任务记录（第一阶段先单任务）
│   └── reports/              # 报告输出
├── data/
│   ├── input/                # 任务输入文本/JSON
│   ├── lens_files/           # 用户提供的 Zemax 文件（可入库）
│   └── golden_case/          # 基准验证案例
├── output/
│   ├── checkpoints/          # 优化检查点 zmx
│   └── reports/              # 生成的报告
└── tests/
```

---

## 6. 环境自检清单

开发前建议先跑一次环境自检：

| 检查 | 通过标准 |
|---|---|
| Python 可用 | 能进入共享 venv |
| pythonnet 可用 | 能 import clr |
| ZOS-API DLL 定位 | 三个 DLL 均找到 |
| 连接 Zemax | 取得 Application 与 PrimarySystem |
| 读取镜头 | 能读面数、波长、视场、光阑面 |
| 与 GUI 核对 | 读取值与 Zemax GUI 一致 |

第一个可运行目标就是这张表全部通过，再进入设计自动化。

---

## 7. 开发起步步骤

建议顺序：

```text
1. 确认 Zemax / Python / pythonnet 版本
2. 复用 zos_connect 思路实现连接与读取
3. 用一个 golden case 验证读取和评价正确
4. 实现系统设置（波长/视场/孔径/像面）+ 回读
5. 实现 Merit Function 构建 + 导出
6. 实现单任务优化闭环
7. 实现指标评价与报告
8. 人工确认后写入经验库
```

---

## 8. 当前决策与后续确认项

当前决策：

1. 技术栈、连接方案、共享 venv 与工作区 tol 工程完全一致，不换技术栈；
2. 第一阶段用 Interactive Extension 模式；
3. 第一阶段只做顺序模式、单任务闭环；
4. 代码骨架就建在 `zemax-skills-opt_design` 目录下（待正式开工时创建）。

后续需要你提供或确认：

1. Zemax 具体版本和授权级别（后续提供）；
2. golden case 用哪个已完成镜头文件（后续提供）。
